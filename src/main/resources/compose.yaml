version: "3.0"
services:
  postgres:
    image: 'postgres:latest'
    environment:
      POSTGRES_DB: "bank"
      POSTGRES_PASSWORD: "password"
      POSTGRES_USER: "user"
    ports:
      - '5432:5432'
